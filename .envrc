#!/usr/bin/env bash

# Machine-local config lives in the parent .envrc of the orbs-rynkowsg checkout
# that holds the orbs as submodules. Inherit it rather than repeat it per orb.
source_up_if_exists
